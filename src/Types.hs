{-
  THE BRAIN (Data Models & Events)

  This file defines 'The Model'—a big record that holds the entire state
  of our app (e.g., the current image, slider positions, and file paths).

  It also defines 'AppEvent', which are messages like "UserClickedBlur"
  or "ImageLoaded". Every file in our project will likely import
  this one so they know what our data looks like.
-}